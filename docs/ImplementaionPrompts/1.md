You are a senior .NET 8 architect.

Generate a clean production-ready solution skeleton for a project called "YtProducer".

Architecture requirements:

- .NET 8
- ASP.NET Core Web API (orchestrator)
- Background Worker Service
- Clean Architecture style separation
- EF Core (PostgreSQL)
- Minimal API style (not controllers)
- Dependency Injection configured
- No business logic yet — only structure and placeholders
- No unnecessary boilerplate
- Clean folder structure
- Production-ready Docker support
- Designed for long-running job orchestration (music, image, video generation)

Project structure must be exactly:

YtProducer/
│
├── src/
│   ├── YtProducer.Api/
│   ├── YtProducer.Worker/
│   ├── YtProducer.Domain/
│   ├── YtProducer.Infrastructure/
│   └── YtProducer.Contracts/
│
├── ui/
│
├── docker/
│   ├── docker-compose.yml
│   └── postgres/
│
└── README.md


========================
REQUIREMENTS PER PROJECT
========================

1) YtProducer.Domain

- Entities:
  - Playlist
  - Track
  - Job
- Enums:
  - PlaylistStatus
  - TrackStatus
  - JobStatus
  - JobType
- No EF Core references here.
- Pure domain models only.

2) YtProducer.Contracts

- DTOs:
  - CreatePlaylistRequest
  - PlaylistResponse
  - TrackResponse
  - JobResponse
- Keep simple.

3) YtProducer.Infrastructure

- YtProducerDbContext (EF Core)
- Configured for PostgreSQL
- DbSets for Playlist, Track, Job
- Entity configurations using Fluent API
- Placeholder IMcpClient interface
- Placeholder McpClient implementation
- JobQueueService (simple DB polling service placeholder)

4) YtProducer.Api

- Minimal API project
- Program.cs configures:
    - DbContext
    - Infrastructure services
- Endpoints:
    POST /playlists
    GET  /playlists
    GET  /playlists/{id}
- Endpoints must be async
- No logic — just placeholders returning fake data

5) YtProducer.Worker

- BackgroundService
- Polls DB for pending jobs
- Logs job execution (no real MCP calls yet)
- Uses scoped services correctly
- Proper cancellation token usage

6) docker/docker-compose.yml

- Postgres service
- API service
- Worker service
- Environment variables for connection string
- Correct service dependencies

7) ui/

- Vite + React skeleton
- Dashboard page
- Shows:
    - List of playlists
    - Status badge
- Simple fetch call to API

8) README.md

Must include:
- Architecture overview
- How to run with docker
- Future roadmap:
    - MCP integration
    - Retry policies
    - Distributed scaling

========================
IMPORTANT RULES
========================

- Use .NET 8 SDK style projects
- Use nullable reference types
- Use implicit usings
- Use clean namespaces
- Use async everywhere
- No TODO spam
- No fake unnecessary complexity
- No MediatR
- No external job queue (we use DB polling)
- Keep code clean and readable

Output format:

- Provide full folder tree
- Then provide each file with clear file path headers
- Do not skip files
- Do not summarize
- Generate actual compilable code